# ZAP baseline configuration for Reading List API
# Disable some checks that may cause false positives for API testing

# Disable checks for traditional web apps
# Traditional App, Session Management = 40012
# Session Management = 40003
exclude=40012,40003

# Increase timeout for slow responses
connection.timeoutInSecs=60
